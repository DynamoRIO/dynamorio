thank you for testing the client interface
#if +RUNREGRESSION_XP +PROGRAM_SHEPHERDING : +-security +-no_executable_if_rx_text --executable_after_load
# SEC_VIO_STOP
No such process found.
Window not found: Untitled - Notepad
#else
# if +DEBUG :
Process notepad.exe, running SC debug
# else
#  if +PROFILE :
Process notepad.exe, running SC profile
#  else
Process notepad.exe, running SC release
#  endif
# endif
nudge delivered 10
nudge delivered 11
# if +DEBUG :
Process notepad.exe, running SC debug
# else
#  if +PROFILE :
Process notepad.exe, running SC profile
#  else
Process notepad.exe, running SC release
#  endif
# endif
Close message sent.
done
#endif
No such process found.
