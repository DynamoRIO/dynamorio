// we don't expect anyone to run w/ -throw_exception or -kill_thread in
// addition to -no_executable_if_rx_text
#if +PROGRAM_SHEPHERDING : +-security +-no_executable_if_rx_text
# if : --detect_mode
#  SEC_VIO_AUTO_STOP
#  if : --internal_detach_mask
#   STOP
#  endif
# else
#  SEC_VIO_CONT
# endif
#endif
iters is 40
// FIXME: strange behavior: giving up on smaller categories
#if : +-desktop || : +-disable_traces --no_coarse_units
Memory check: pagefile usage increase is < 80 KB
#else
#if : +-no_coarse_units
Memory check: pagefile usage increase is < 80 KB
#else
Memory check: pagefile usage increase is < 80 KB
#endif
#endif
sum=-994385352
