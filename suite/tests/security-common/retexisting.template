starting good function
in foo
first foo a=1
in foo
second foo a=2
starting bad function
looking at ring
#if +PROGRAM_SHEPHERDING : +-security --no_ret_after_call --C --X --low --client --thin_client --use_moduledb
# if : --detect_mode
#  SEC_VIO_AUTO_STOP
#  if : --internal_detach_mask
#   STOP
#  endif
# else
#  SEC_VIO_CONT
# endif
#endif
M-m-my PRECIOUS is stolen! ATTACK SUCCESSFUL!
