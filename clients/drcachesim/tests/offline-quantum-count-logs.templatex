\[heartbeat\] Quantum [0-9]* in TID [0-9]*: instrs delta = [0-9]*, snapshot = [0-9]*
Basic counts tool results:
Total counts:
     .* total \(fetched\) instructions
     .* total unique \(fetched\) instructions
     .* total non-fetched instructions
     .* total prefetches
     .* total data loads
     .* total data stores
     .* total icache flushes
     .* total dcache flushes
           1 total threads
     .* total scheduling markers
     .* total transfer markers
     .* total function id markers
     .* total function return address markers
     .* total function argument markers
     .* total function return value markers
           0 total physical address \+ virtual address marker pairs
           0 total physical address unavailable markers
     .* total other markers
     .* total encodings
Quantum 0 deltas:.*
     .* quantum delta \(fetched\) instructions
     .* quantum delta unique \(fetched\) instructions
     .* quantum delta non-fetched instructions
     .* quantum delta prefetches
     .* quantum delta data loads
     .* quantum delta data stores
     .* quantum delta icache flushes
     .* quantum delta dcache flushes
     .* quantum delta scheduling markers
     .* quantum delta transfer markers
     .* quantum delta function id markers
     .* quantum delta function return address markers
     .* quantum delta function argument markers
     .* quantum delta function return value markers
           0 quantum delta physical address \+ virtual address marker pairs
           0 quantum delta physical address unavailable markers
     .* quantum delta other markers
     .* quantum delta encodings
.*
Quantum 0 snapshots:
     .* quantum snapshot \(fetched\) instructions
     .* quantum snapshot unique \(fetched\) instructions
     .* quantum snapshot non-fetched instructions
     .* quantum snapshot prefetches
     .* quantum snapshot data loads
     .* quantum snapshot data stores
     .* quantum snapshot icache flushes
     .* quantum snapshot dcache flushes
     .* quantum snapshot scheduling markers
     .* quantum snapshot transfer markers
     .* quantum snapshot function id markers
     .* quantum snapshot function return address markers
     .* quantum snapshot function argument markers
     .* quantum snapshot function return value markers
           0 quantum snapshot physical address \+ virtual address marker pairs
           0 quantum snapshot physical address unavailable markers
     .* quantum snapshot other markers
     .* quantum snapshot encodings
.*
Thread .* counts:
     .* \(fetched\) instructions
     .* unique \(fetched\) instructions
     .* non-fetched instructions
     .* prefetches
     .* data loads
     .* data stores
     .* icache flushes
     .* dcache flushes
     .* scheduling markers
     .* transfer markers
     .* function id markers
     .* function return address markers
     .* function argument markers
     .* function return value markers
           0 physical address \+ virtual address marker pairs
           0 physical address unavailable markers
     .* other markers
     .* encodings
